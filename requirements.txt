fastapi>=0.68.0
uvicorn[standard]>=0.15.0
requests>=2.26.0
pandas==1.1.5 # Pinned to latest compatible with Python 3.6
numpy==1.19.5 # Pinned to latest compatible with Python 3.6
scikit-learn==0.24.2 # Pinning for consistency
joblib>=1.0.1
python-dotenv>=0.19.0
overpass>=0.7
aiofiles>=0.7.0
jinja2>=3.0.1,<3.1 # Allow 3.0.x, but not 3.1+
pydantic>=1.8.2,<2.0 # Allow 1.x, but not 2.0+
typing-extensions>=3.10.0 # Keep as is, or pin if needed